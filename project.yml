name: MyBody
options:
  bundleIdPrefix: brickverse
  # 主开发语言 = 简体中文；对齐 ASC 的 Primary Language，避免 Xcode 后续自动
  # 把 CFBundleDevelopmentRegion 设成 en。未来追加英文本地化时，原文(中文)会
  # 作为 fallback。
  developmentLanguage: "zh-Hans"
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "15.0"
  createIntermediateGroups: true

schemes:
  MyBody:
    build:
      targets:
        MyBody: all
        MyBodyTests: [test]
        MyBodyUITests: [test]
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - MyBodyTests
        - MyBodyUITests
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release

targets:
  MyBody:
    type: application
    platform: iOS
    sources:
      - path: MyBody
    dependencies:
      - sdk: HealthKit.framework
    postCompileScripts:
      - name: "Embed Git Commit Hash"
        # 在编译后、签名前，把当前 git short hash 写进已生成的 Info.plist。
        # 这样设置页就能显示 commit hash，便于判断真机 / TestFlight 上跑的是哪一次提交。
        # 如果有未提交改动，hash 后会附加 "-dirty"。非 git 仓库或缺 git 时写 "unknown"。
        script: |
          set -e
          if command -v git >/dev/null 2>&1 && git -C "$SRCROOT" rev-parse --git-dir >/dev/null 2>&1; then
            GIT_HASH=$(git -C "$SRCROOT" rev-parse --short HEAD 2>/dev/null || echo "unknown")
            if ! git -C "$SRCROOT" diff --quiet HEAD 2>/dev/null; then
              GIT_HASH="${GIT_HASH}-dirty"
            fi
          else
            GIT_HASH="unknown"
          fi
          INFO_PLIST="${TARGET_BUILD_DIR}/${INFOPLIST_PATH}"
          if [ -f "$INFO_PLIST" ]; then
            /usr/libexec/PlistBuddy -c "Delete :GitCommitHash" "$INFO_PLIST" 2>/dev/null || true
            /usr/libexec/PlistBuddy -c "Add :GitCommitHash string ${GIT_HASH}" "$INFO_PLIST"
            echo "[Embed Git] GitCommitHash=${GIT_HASH} -> $INFO_PLIST"
          else
            echo "[Embed Git] WARN: Info.plist not found at $INFO_PLIST"
          fi
        basedOnDependencyAnalysis: false
    settings:
      base:
        INFOPLIST_FILE: MyBody/Info.plist
        CODE_SIGN_ENTITLEMENTS: MyBody/MyBody.entitlements
        PRODUCT_BUNDLE_IDENTIFIER: brickverse.MyBodyApp
        PRODUCT_NAME: 身记
        # PRODUCT_NAME 是中文 "身记"，Swift 模块名默认会被派生成非法标识符。
        # 显式锁定为 MyBody，保证 `@testable import MyBody` 能解析。
        PRODUCT_MODULE_NAME: MyBody
        DEVELOPMENT_TEAM: "${DEVELOPMENT_TEAM}"
        CODE_SIGN_STYLE: Automatic
        SUPPORTS_MACCATALYST: NO
        SUPPORTS_MAC_DESIGNED_FOR_IPHONE_IPAD: YES
        MARKETING_VERSION: "1.0"
        CURRENT_PROJECT_VERSION: "1"
        SWIFT_VERSION: "5.9"
        TARGETED_DEVICE_FAMILY: "1"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        GENERATE_INFOPLIST_FILE: YES
        INFOPLIST_KEY_NSPhotoLibraryUsageDescription: "用于自动识别相册中的 InBody 报告并录入健康数据。"
        INFOPLIST_KEY_NSCameraUsageDescription: "用于拍摄 InBody 报告并自动识别健康数据。"
        INFOPLIST_KEY_NSHealthUpdateUsageDescription: "用于将识别出的体重数据同步到「健康」App，方便统一查看与跨 App 共享。"
        INFOPLIST_KEY_NSHealthShareUsageDescription: "用于读取「健康」App 中的体重数据，确认同步是否成功。"
        INFOPLIST_KEY_UIApplicationSceneManifest_Generation: YES
        INFOPLIST_KEY_UIApplicationSupportsIndirectInputEvents: YES
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        INFOPLIST_KEY_UISupportedInterfaceOrientations: UIInterfaceOrientationPortrait
        INFOPLIST_KEY_CFBundleDisplayName: "身记"
        # 声明 App 不使用任何非豁免加密,避免每次 TestFlight 上传弹出 Export
        # Compliance 问卷;对应 Info.plist 中的 ITSAppUsesNonExemptEncryption=NO。
        INFOPLIST_KEY_ITSAppUsesNonExemptEncryption: "NO"

  MyBodyUITests:
    type: bundle.ui-testing
    platform: iOS
    sources:
      - path: MyBodyUITests
    dependencies:
      - target: MyBody
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: brickverse.MyBodyAppUITests
        DEVELOPMENT_TEAM: "${DEVELOPMENT_TEAM}"
        CODE_SIGN_STYLE: Automatic
        SWIFT_VERSION: "5.9"
        TARGETED_DEVICE_FAMILY: "1"
        GENERATE_INFOPLIST_FILE: YES
        TEST_TARGET_NAME: MyBody

  MyBodyTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: MyBodyTests
    dependencies:
      - target: MyBody
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: brickverse.MyBodyAppTests
        DEVELOPMENT_TEAM: "${DEVELOPMENT_TEAM}"
        CODE_SIGN_STYLE: Automatic
        SWIFT_VERSION: "5.9"
        TARGETED_DEVICE_FAMILY: "1"
        GENERATE_INFOPLIST_FILE: YES
        # 宿主 App 的 PRODUCT_NAME 是中文 "身记"，xcodegen 自动派生的
        # TEST_HOST / BUNDLE_LOADER 用了项目名 MyBody，需手动指向真实产物。
        TEST_HOST: "$(BUILT_PRODUCTS_DIR)/身记.app/身记"
        BUNDLE_LOADER: "$(TEST_HOST)"
        # MyBodyTests/Fixtures/**/*.heic 等图片文件会被自动识别为 resources
        # 并拷贝进测试 bundle（参见 README.md 与 OCRServiceInBody230Tests）。
